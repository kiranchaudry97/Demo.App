<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Demo App</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
    <style>
        @Html.Raw(@"/* Reduce overall size and vertical spacing so page fits better at 100% zoom */
        body { font-size: 0.82rem; line-height: 1.12; }

        /* Limit width on large screens to avoid very wide layout */
        @media (min-width: 1200px) { .container-fluid { max-width: 950px; } }

        /* Reduce large header spacing used in views */
        header.my-4 { margin-top: 0.2rem !important; margin-bottom: 0.4rem !important; }
        h1 { font-size: 1.1rem; }

        /* Reduce spacing between sections */
        .mb-4 { margin-bottom: 0.4rem !important; }

        /* Make tables more compact */
        .table td, .table th { padding: 0.3rem; }

        /* Tighten forms and buttons spacing */
        .form-control { padding: 0.2rem 0.35rem; font-size: 0.85rem; }
        .btn { padding: 0.28rem 0.45rem; font-size: 0.8rem; }
        " )
    </style>
</head>
<body>
    <div class="container-fluid px-4">
        <header class="my-4">
            <h1>@ViewData["Title"]</h1>
        </header>
        <main>
            @RenderBody()
        </main>
        <footer class="text-center my-4">
            <p>&copy; 2026 - Demo App</p>
        </footer>
    </div>
</body>
</html>