Demo.MauiApp - MAUI shell project that references `Demo.SharedModels` for shared domain models. This is a minimal starter: replace mock data with API calls or embed local Sqlite DB using EF Core.

To run: open in Visual Studio 2022/2023 with MAUI workloads, set `Demo.MauiApp` as startup project.